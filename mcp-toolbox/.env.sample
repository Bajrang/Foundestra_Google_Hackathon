# Database connection settings for loader script
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres

# Optional: override Cloud SQL connection string or use Cloud SQL proxy
# Example: CLOUD_SQL_INSTANCE=project:region:instance
