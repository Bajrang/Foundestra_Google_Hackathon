server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: increase buffer/timeouts if needed
    sendfile on;
    tcp_nopush on;
    keepalive_timeout 65;
}